name: my_taskify
description: "A new Flutter project."

# Prevent package from being accidentally published to pub.dev
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# Version and build number
version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter

  # Common packages
  cupertino_icons: ^1.0.8
  flutter_slidable: ^3.1.1
  flutter_native_splash: ^2.4.3
  image_picker: ^0.8.5+3  # Updated to the latest stable version
  shared_preferences: ^2.0.15

  # Optional - Flutter launcher icons package
  flutter_launcher_icons: ^0.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting package for better code quality
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/icon.png"

flutter:
  uses-material-design: true

  assets:
   - assets/images/icon.png

  # Correct asset configuration
  # Ensure this is the correct file path

  # Fonts section (if any custom fonts are needed)
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
